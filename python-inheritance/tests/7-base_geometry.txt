The ``7-base_geometry`` module
===========================

Using ``BaseGeometry``:

    >>> BaseGeometry = __import__("7-base_geometry").BaseGeometry
    >>> bg = BaseGeometry()

    >>> # 1. Test present: area()
    >>> try:
    ...     bg.area()
    ... except Exception as e:
    ...     print("[{}] {}".format(e.__class__.__name__, e))
    [Exception] area() is not implemented

    >>> # 2. Test present: integer_validator() handling normal case
    >>> bg.integer_validator("my_int", 12)

    >>> # 3. Test present: integer_validator("age") normal case
    >>> bg.integer_validator("age", 1)

    >>> # 4. Test present: integer_validator("age", 0)
    >>> try:
    ...     bg.integer_validator("age", 0)
    ... except ValueError as e:
    ...     print(e)
    age must be greater than 0

    >>> # 5. Test present: integer_validator("age", -4)
    >>> try:
    ...     bg.integer_validator("age", -4)
    ... except ValueError as e:
    ...     print(e)
    age must be greater than 0

    >>> # 6. Test present: integer_validator("age", "4")
    >>> try:
    ...     bg.integer_validator("age", "4")
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

    >>> # 7. Test present: integer_validator("age", (4,))
    >>> try:
    ...     bg.integer_validator("age", (4,))
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

    >>> # 8. Test present: integer_validator("age", [3])
    >>> try:
    ...     bg.integer_validator("age", [3])
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

    >>> # 9. Test present: integer_validator("age", True)
    >>> try:
    ...     bg.integer_validator("age", True)
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

    >>> # 10. Test present: integer_validator("age", (3, 4))
    >>> try:
    ...     bg.integer_validator("age", (3, 4))
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

    >>> # 11. Test present: integer_validator("age", None)
    >>> try:
    ...     bg.integer_validator("age", None)
    ... except TypeError as e:
    ...     print(e)
    age must be an integer
